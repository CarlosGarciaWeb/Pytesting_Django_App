[pytest]
DJANGO_SETTINGS_MODULE = technewscorp.settings
python_files = test_*.py
addopts = -s -v --durations=0 -rs --cov=.
markers =
    performance: marks test as a performance (deselect with '-m "not performance"')
filterwarnings =
    ignore::_pytest.warning_types.PytestUnknownMarkWarning
